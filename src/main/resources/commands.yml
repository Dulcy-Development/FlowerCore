#
#
#               FlowerCore made by Emmiesa | Commands.yml
#
#
vanilla-commands-blocker:
  perm-to-bypass: "flower.blockedcmd.bypass"
  disable_commands:
    - "/me"
    - "/say"
    - "/minecraft:me"
    - "/minecraft:say"
    - "/minecraft:tell"
    - "/achievement"
    - "/minecraft:achievement"
    - "/bukkit:?"
    - "/version FlowerCore"
    - "/ver FlowerCore"

#This overwrites /ver, /bukkit:ver /version, /bukkit:version, /about, /bukkit:about, /? and sends the following message.
version-command:
  enabled: true
  perm-to-bypass: "flower.bypasscmd.ver"
  message:
    - "&cYou don't have permission to execute this command!"
    #- "&fHey &b%player%&f,"
    #- "&fThis server is running &b&l%spigot% &fusing Emmy's awesome core plugin which is called &b&lFlowerCore&r!"
    #- " &8- &7%version%"
    #- ""

bungee-command:
  enabled: true
  perm-to-bypass: "flower.bypasscmd.bungee"
  message:
    - "&cYou don't have permission to execute this command!"

plugins-command:
  enabled: true
  perm-to-bypass: "flower.bypasscmd.pl"
  message:
    - "&cYou don't have permission to execute this command!"
