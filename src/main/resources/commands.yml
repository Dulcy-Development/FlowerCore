#
#
#               FlowerCore made by Emmiesa | Commands.yml
#
#
#Based on this Configuration, the commands listed below, will either register, or not.
plugin:
  enabled: true
  permission:
    perm-off: false #Does not require the perm if 'true'
    perm: flower.cmd.plugin

    #test